/**
 * Application configuration
 */

const config = {
  // API base URL for external services
  apiBaseUrl: 'http://localhost:8000', // Default to local development server
  
  // Other configuration settings can be added here
};

export default config; 